apiVersion: v1
kind: Service
metadata:
  name: app-service
  labels:
    owner: IlyaBikmetov
spec:
  selector:
    project: proj
  ports:
    - name      : app-listener
      protocol  : TCP
      port      : 8000
      targetPort: 8000
  type: LoadBalancer